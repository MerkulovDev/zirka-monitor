# 📱 Zirka Monitor

Автоматичний моніторинг змін у графіку відключень е.е. київ.обл з повідомленнями в Telegram.

## 📋 Опис

Проект для автоматичного моніторингу змін з повідомленнями в Telegram.

### Особливості

- ✅ Автоматичний моніторинг через GitHub Actions
- ✅ Миттєві сповіщення в Telegram при змінах
- ✅ Збереження історії змін
- ✅ Детальні логи помилок

## 🚀 Встановлення

### Локально

1. Клонуйте репозиторій:
```bash
git clone <repository-url>
cd zirka-monitor
```

2. Встановіть залежності:
```bash
npm install
```

3. Налаштуйте змінні середовища:
```bash
export TELEGRAM_BOT_TOKEN="ваш_токен_бота"
export TELEGRAM_CHAT_ID="ваш_chat_id"
export ADDRESS_CITY="ваше_місто"
export ADDRESS_STREET="ваша_вулиця"
export ADDRESS_HOUSE="ваш_номер_будинку"
```

4. Запустіть моніторинг:
```bash
npm start
```

### GitHub Actions

Для автоматичного моніторингу через GitHub Actions:

1. Форкніть або клонуйте цей репозиторій
2. Налаштуйте GitHub Secrets в налаштуваннях репозиторію:
   - `Settings` → `Secrets and variables` → `Actions` → `New repository secret`
   - Додайте `TELEGRAM_BOT_TOKEN` з токеном вашого Telegram бота
   - Додайте `TELEGRAM_CHAT_ID` з ID чату або каналу
   - Додайте `ADDRESS_CITY`, `ADDRESS_STREET`, `ADDRESS_HOUSE` з вашою адресою

3. Workflow запускатиметься автоматично кожну годину

## 🤖 Telegram Bot Setup

### Отримання токену бота

1. Створіть бота через [@BotFather](https://t.me/BotFather)
2. Відправте `/newbot` та слідуйте інструкціям
3. Скопіюйте токен (виглядає як `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

### Отримання Chat ID

#### Для особистих повідомлень:

1. Відправте боту `/start`
2. Відкрийте браузер і перейдіть:
```
https://api.telegram.org/botВАШ_ТОКЕН/getUpdates
```
3. Знайдіть `"chat":{"id":123456789}`
4. Скопіюйте це число

#### Для каналів:

1. Створіть канал або використайте існуючий
2. Додайте бота як адміністратора каналу
3. Отримайте chat_id через:
```
https://api.telegram.org/botВАШ_ТОКЕН/getUpdates
```
4. Для публічних каналів використайте формат: `@ім'я_каналу`
5. Для приватних каналів використайте ID (зазвичай негативне число)

## 📁 Структура проекту

```
zirka-monitor/
├── .github/
│   └── workflows/
│       └── monitor.yml          # GitHub Actions workflow
├── src/
│   ├── config.js                # Конфігурація та перевірки
│   ├── telegram.js              # Робота з Telegram
│   ├── schedule.js              # Обробка графіків
│   ├── state.js                 # Робота зі станом
│   └── scraper.js               # Скрапінг даних
├── data/
│   └── last_known_schedule.json  # Збережений стан
├── monitor.js                    # Головний скрипт моніторингу
├── package.json                 # Залежності
├── .gitignore
└── README.md
```

## ⚙️ Налаштування

### Зміна частоти моніторингу

Відредагуйте `.github/workflows/monitor.yml`:

```yaml
schedule:
  - cron: '0 * * * *'  # Кожну годину
  # - cron: '*/30 * * * *'  # Кожні 30 хвилин
  # - cron: '0 */2 * * *'   # Кожні 2 години
```

## 🔍 Як це працює

1. **Запуск**: Скрипт запускається через GitHub Actions
2. **Перевірка**: Перевіряє актуальні дані
3. **Порівняння**: Порівнює з попередньо збереженим станом
4. **Сповіщення**: Відправка в Telegram при виявленні змін
5. **Збереження**: Збереження нового стану для наступної перевірки

## 📝 Логування

Скрипт виводить детальні логи:

- ✅ Успішні операції
- ⚠️ Попередження
- ❌ Помилки
- 📢 Виявлення змін

## 🛠️ Розв'язання проблем

### Telegram сповіщення не приходять

1. Перевірте правильність токену бота
2. Перевірте `TELEGRAM_CHAT_ID`
3. Переконайтесь, що бот додано в канал/чат
4. Перегляньте логи в GitHub Actions

## 📊 GitHub Actions

Workflow можна запустити вручну через вкладку `Actions`:

1. Перейдіть до `Actions` в репозиторії
2. Виберіть workflow `Monitor`
3. Натисніть `Run workflow`
4. Виберіть гілку (`main` / `master`)
5. Натисніть `Run workflow`

## 📅 Графік роботи

За замовчуванням моніторинг запускається **кожну годину**.

Можна змінити через налаштування cron у `.github/workflows/monitor.yml`.

## 🤝 Внески

Будь-які покращення та зміни вітаються! Створюйте Issues та Pull Requests.

## 📄 Ліцензія

MIT

---

**Створено для автоматизації моніторингу**
